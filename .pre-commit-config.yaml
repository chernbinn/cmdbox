repos:
- repo: local
  hooks:
    - id: freeze-version
      name: Freeze version
      entry: python -m scripts.git_versioning_callback commit
      language: system
      stages: [commit-msg]
      verbose: true